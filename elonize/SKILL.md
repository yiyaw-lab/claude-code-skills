---
description: Alias for /raze — apply Elon's principles (Musk's Algorithm: question requirements → delete → simplify → accelerate → automate; idiot index; the best part is no part). The razor (delete to the simplest sufficient form) is the core move; this is the growing basket of those first-principles rules. Modes: <target> (scan+propose) | audit | apply.
argument-hint: "<path|module|feature|process> | audit | apply  (default: scan + propose)"
allowed-tools: Read, Edit, Write, Bash
---

Alias for **/raze**. Follow `~/.claude/commands/raze.md` exactly — same modes (`<target>` | `audit` | `apply`), same propose-on-approval + ≥80-unused-confidence gate + per-cut tests-green + hard refuse-list safety, same two-sided add-back-rate metric.

Why two names: **raze** is the verb — apply the razor, raze the unnecessary to the simplest sufficient form (self-documenting: a cold reader infers the method). **elonize** is the basket — the razor is ONE of Elon's principles; as more get encoded (idiot index, best-part-is-no-part, first-principles requirement teardown, the accelerate/automate ordering), they accrue under this name. Use whichever reads truer in the moment; they run the identical procedure today.
